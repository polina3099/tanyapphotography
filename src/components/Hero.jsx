import './Hero.css'

function Hero() {
  return (
    <section className="hero">
      <div className="hero-text">
        <p className="hero-sub">capturing moments</p>
        <h1 className="hero-title">Tanya P.</h1>
        <p className="hero-sub">photography</p>
      </div>
    </section>
  )
}

export default Hero